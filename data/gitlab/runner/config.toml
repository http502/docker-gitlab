concurrent = 1
check_interval = 0

[session_server]
  session_timeout = 1800

[[runners]]
  name = "9d87d90f51a3"
  url = "http://lab.tog.co.id:8000/"
  token = "im8oLhTBzQH4BM_1Qrs9"
  executor = "docker"
  environment = ["GIT_STRATEGY=clone", "REGISTER_LOCKED=false"]
  [runners.custom_build_dir]
  [runners.docker]
    tls_verify = false
    image = "alpine:latest"
    privileged = false
    disable_entrypoint_overwrite = false
    oom_kill_disable = false
    disable_cache = false
    volumes = ["/cache"]
    shm_size = 0
  [runners.cache]
    [runners.cache.s3]
    [runners.cache.gcs]
